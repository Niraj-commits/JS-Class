let form = document.getElementById("form")
let field = document.getElementById("input")
let list= document.getQueryElement(".list")

form.addEventListener("submit",function(e){

    e.preventDefault;
    createText()
})

 function.createItem